# Table of contents — Agent Package Manager book (machine-readable source of truth).
#
# STATUS: awaiting architecture. The previous chapters were cleared when the repository was
# reframed to Agent Package Manager. The `book-architect` populates `chapters` below from
# `content/book-brief.md`, then `frontend-builder` runs `site/generate.py` to render the shell.
#
# Schema per chapter (fill in when authoring):
#   - id: 01-<slug>            # stable id, "NN-<slug>"
#     number: 1                # 1-based order
#     title: <human title>
#     slug: <url-slug>         # file name in site/chapters/<slug>.html
#     objective: <one sentence: what the reader can do afterward>
#     apm_features:            # APM features/commands/manifest keys this chapter covers (may be [])
#       - <feature>
#     depends_on:              # ids of prerequisite chapters (may be [])
#       - <chapter-id>
#     sections:                # ordered in-page section anchors
#       - Objective
#       - Concept/Theory
#       - In APM
#       - When to use / pitfalls
#       - Worked example
#       - Recap & next

chapters: []
